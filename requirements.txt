# Runtime dependencies — what the application needs to *run*.
#
# Kept at the repository root because hosted platforms (Streamlit Community Cloud among
# them) install this file automatically. Development extras — the evaluation stack and
# test tooling — live in requirements-dev.txt, so a deployment never pulls ~130 packages
# it will not use.
#
#   pip install -r requirements.txt          # run the app
#   pip install -r requirements-dev.txt      # everything, for development
#
# CPU-only torch. Without this index, sentence-transformers resolves the CUDA build:
# gigabytes of GPU libraries a CPU host cannot use, and enough to risk a build timeout.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.13.0

streamlit==1.60.0

chromadb==1.5.9
ddgs==9.14.4
groq==0.37.1
langchain==1.3.14
langchain-core==1.5.1
langchain-groq==1.1.3
langchain-text-splitters==1.1.2
langfuse==4.14.1
langgraph==1.2.9
pdfplumber==0.11.10
pillow==12.3.0
pydantic==2.13.4
pymupdf==1.28.0
python-docx==1.2.0
python-dotenv==1.2.2
rank-bm25==0.2.2
sentence-transformers==5.6.1

# Serving the HTTP API. Not needed by the Streamlit UI, which runs the agent in-process.
fastapi==0.140.0
uvicorn==0.51.0
