# Runtime dependencies for the API container only.
#
# Deliberately excludes the evaluation stack (ragas, datasets, pandas, pyarrow),
# the test tooling (pytest), and table extraction (pdfplumber) — none of which the
# serving path imports. Keeping them out of the image saves well over a gigabyte.
#
# Torch is installed separately in the Dockerfile from the CPU-only index, since the
# default wheel bundles CUDA libraries that are dead weight in a CPU container.

chromadb==1.5.9
ddgs==9.14.4
fastapi==0.140.0
groq==0.37.1
langchain-core==1.5.1
langchain-groq==1.1.3
langchain-text-splitters==1.1.2
langfuse==4.14.1
langgraph==1.2.9
pydantic==2.13.4
pymupdf==1.28.0
python-dotenv==1.2.2
rank-bm25==0.2.2
sentence-transformers==5.6.1
uvicorn==0.51.0
