# Runtime dependencies for the container.
#
# Deliberately excludes the evaluation stack (ragas, datasets, pandas, pyarrow) and
# test tooling (pytest) — the serving path imports none of it, and leaving it out
# saves well over a gigabyte.
#
# pdfplumber is included even though serving never uses it: the same image also runs
# the ingestion service in docker-compose, and one image that does both is simpler to
# build, version, and ship than two that differ by 30MB.
#
# Torch is installed separately in the Dockerfile from the CPU-only index, since the
# default wheel bundles CUDA libraries that are dead weight in a CPU container.

chromadb==1.5.9
ddgs==9.14.4
fastapi==0.140.0
groq==0.37.1
# langfuse's LangChain callback handler imports the full `langchain` package at
# runtime, not just langchain-core — a lazy import that static analysis of our own
# source does not reveal. Omitting it breaks tracing inside the container only.
langchain==1.3.14
langchain-core==1.5.1
langchain-groq==1.1.3
langchain-text-splitters==1.1.2
langfuse==4.14.1
langgraph==1.2.9
python-docx==1.2.0
pdfplumber==0.11.10
pydantic==2.13.4
pymupdf==1.28.0
python-dotenv==1.2.2
rank-bm25==0.2.2
sentence-transformers==5.6.1
uvicorn==0.51.0
